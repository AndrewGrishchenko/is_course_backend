package com.andrew.controller;

import jakarta.ws.rs.Path;

@Path("/route/")
public class SegmentController {
    //надо ручку POST /route/{id}/segments и юзать функу с триггером и статусы переписать
}
